services:
  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env

  client:
    build:
      context: .
      dockerfile: client/Dockerfile
    ports:
      - "5173:80"
    environment:
      # If your SPA reads runtime config, you’d handle it here (or via build-time vars).
      # Many SPAs use build-time env vars; treat them intentionally.
      API_BASE_URL: "http://localhost:3000"
